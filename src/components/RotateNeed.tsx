import { Box, Typography } from "@mui/material"

export const Rotate: React.FC = () => {
    return <Box >
        <Typography variant="h4">Works only in landscape for mobile</Typography>
        <Typography variant="h5">Please rotate the device</Typography>
        
    </Box>
}